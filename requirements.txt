# Web framework
fastapi==0.110.0
uvicorn[standard]==0.29.0
pydantic==2.6.4
python-multipart==0.0.20
python-dotenv==1.0.1

# Face verification
insightface==0.7.3
onnxruntime==1.17.1

# Image / document processing
opencv-python-headless==4.9.0.80
numpy==1.26.4
Pillow==11.1.0
requests==2.31.0
pdf2image==1.16.3
python-docx==1.2.0
PyMuPDF==1.24.10

# OCR — Tesseract (fallback, always installed)
pytesseract==0.3.10

# OCR — PaddleOCR (primary engine on x86 Linux production)
# paddlepaddle wheel is ~200 MB; comment both lines out to disable Paddle entirely.
paddlepaddle==2.6.2
paddleocr==2.7.3
